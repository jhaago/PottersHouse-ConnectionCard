const DB_NAME="pottershouse-connection-card";const DB_VERSION=1;const STORE="connections";let db;document.addEventListener("DOMContentLoaded",async()=>{db=await openDb();const recorder=localStorage.getItem("defaultRecorder")||"";document.getElementById("defaultRecorder").value=recorder;document.getElementById("recordedBy").value=recorder;document.getElementById("syncUrl").value=localStorage.getItem("syncUrl")||"";bindNavigation();bindForm();bindSettings();bindSync();window.addEventListener("online",()=>{updateConnectivity();syncPending(false)});window.addEventListener("offline",updateConnectivity);updateConnectivity();await refreshEntries();if("serviceWorker"in navigator)navigator.serviceWorker.register("sw.js").catch(console.error);if(navigator.onLine)syncPending(false)});function openDb(){return new Promise((resolve,reject)=>{const req=indexedDB.open(DB_NAME,DB_VERSION);req.onupgradeneeded=()=>{const database=req.result;if(!database.objectStoreNames.contains(STORE)){const store=database.createObjectStore(STORE,{keyPath:"id"});store.createIndex("createdAt","createdAt",{unique:false})}};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)})}function saveRecord(record){return new Promise((resolve,reject)=>{const tx=db.transaction(STORE,"readwrite");tx.objectStore(STORE).put(record);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)})}function deleteRecord(id){return new Promise((resolve,reject)=>{const tx=db.transaction(STORE,"readwrite");tx.objectStore(STORE).delete(id);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)})}function getAllRecords(){return new Promise((resolve,reject)=>{const req=db.transaction(STORE,"readonly").objectStore(STORE).getAll();req.onsuccess=()=>resolve(req.result||[]);req.onerror=()=>reject(req.error)})}function bindNavigation(){document.querySelectorAll(".nav-button").forEach(button=>button.addEventListener("click",()=>showView(button.dataset.view)))}function showView(id){document.querySelectorAll(".view").forEach(v=>v.classList.remove("active"));document.querySelectorAll(".nav-button").forEach(v=>v.classList.remove("active"));document.getElementById(id).classList.add("active");document.querySelector(`[data-view="${id}"]`).classList.add("active");if(id==="entriesView")refreshEntries()}function bindForm(){document.getElementById("connectionForm").addEventListener("submit",async e=>{e.preventDefault();const record={id:crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random()}`,createdAt:new Date().toISOString(),name:value("name"),phone:value("phone"),email:value("email"),address:value("address"),age:value("age"),firstTimeDecision:value("firstTimeDecision"),recordedBy:value("recordedBy"),notes:value("notes"),permissionToContact:document.getElementById("permissionToContact").checked,synced:false,syncedAt:""};await saveRecord(record);const recorder=record.recordedBy;e.target.reset();document.getElementById("recordedBy").value=recorder||localStorage.getItem("defaultRecorder")||"";showToast("Saved on this phone");await refreshEntries();if(navigator.onLine)syncPending(false)})}function bindSettings(){document.getElementById("saveSettings").addEventListener("click",()=>{const recorder=value("defaultRecorder");const syncUrl=value("syncUrl");localStorage.setItem("defaultRecorder",recorder);localStorage.setItem("syncUrl",syncUrl);document.getElementById("recordedBy").value=recorder;showToast("Settings saved")})}function bindSync(){document.getElementById("syncButton").addEventListener("click",()=>syncPending(true))}function updateConnectivity(){const pill=document.getElementById("statusPill");if(navigator.onLine){pill.textContent="Online";pill.className="status-pill online"}else{pill.textContent="Offline ready";pill.className="status-pill offline"}}async function refreshEntries(){const records=(await getAllRecords()).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));const pending=records.filter(r=>!r.synced).length;document.getElementById("entriesSummary").textContent=`${records.length} saved · ${pending} waiting to sync`;const badge=document.getElementById("queueBadge");badge.textContent=pending;badge.classList.toggle("hidden",pending===0);const list=document.getElementById("entriesList");list.innerHTML="";document.getElementById("emptyState").classList.toggle("hidden",records.length!==0);for(const r of records){const when=new Date(r.createdAt).toLocaleString([],{day:"numeric",month:"short",hour:"numeric",minute:"2-digit"});const node=document.createElement("article");node.className="entry";node.innerHTML=`<div class="entry-top"><div><div class="entry-name">${escapeHtml(r.name||"Unnamed")}</div><div class="entry-meta">${escapeHtml(when)}${r.phone?" · "+escapeHtml(r.phone):""}${r.recordedBy?"<br>By "+escapeHtml(r.recordedBy):""}</div></div><span class="sync-state ${r.synced?"synced":"waiting"}">${r.synced?"✓ Synced":"Waiting"}</span></div>${r.synced?"":`<div class="entry-actions"><button class="danger" data-delete="${r.id}">DELETE</button></div>`}`;list.appendChild(node)}document.querySelectorAll("[data-delete]").forEach(button=>button.addEventListener("click",async()=>{if(!confirm("Delete this unsynced entry from this phone?"))return;await deleteRecord(button.dataset.delete);await refreshEntries();showToast("Entry deleted")}))}async function syncPending(showMessages=true){const url=(localStorage.getItem("syncUrl")||"").trim();if(!navigator.onLine){if(showMessages)showToast("No internet — entries are safe on this phone");return}if(!url){if(showMessages){showToast("Add the Google sync URL in Settings first");showView("settingsView")}return}const records=await getAllRecords();const pending=records.filter(r=>!r.synced);if(!pending.length){if(showMessages)showToast("Everything is already synced");return}if(showMessages)showToast(`Syncing ${pending.length} entr${pending.length===1?"y":"ies"}...`);let success=0;for(const record of pending){try{const response=await fetch(url,{method:"POST",headers:{"Content-Type":"text/plain;charset=utf-8"},body:JSON.stringify(record)});if(!response.ok)throw new Error(`HTTP ${response.status}`);const result=await response.json();if(!result.ok)throw new Error(result.error||"Sync rejected");record.synced=true;record.syncedAt=new Date().toISOString();await saveRecord(record);success++}catch(error){console.error(error);break}}await refreshEntries();if(showMessages)showToast(success===pending.length?`Synced ${success} entr${success===1?"y":"ies"}`:`${success} synced. Remaining entries are safe on this phone.`)}function value(id){return document.getElementById(id).value.trim()}function showToast(message){const toast=document.getElementById("toast");toast.textContent=message;toast.classList.remove("hidden");clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>toast.classList.add("hidden"),2600)}function escapeHtml(text){return String(text).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}