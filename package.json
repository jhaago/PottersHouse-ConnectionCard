{
  "name": "pottershouse-connection-card",
  "version": "0.2.1-xcode15",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:native": "node scripts/prepare-native.mjs",
    "apply:native-icons": "node scripts/apply-native-icons.mjs",
    "cap:add:android": "npm run prepare:native && npx cap add android && npm run apply:native-icons",
    "cap:sync:android": "npm run prepare:native && npx cap sync android && npm run apply:native-icons",
    "android:open": "npm run cap:sync:android && npx cap open android",
    "cap:add:ios": "npm run prepare:native && npx cap add ios && npm run apply:native-icons",
    "cap:sync:ios": "npm run prepare:native && npx cap sync ios && npm run apply:native-icons",
    "ios:open": "npm run cap:sync:ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "6.2.2",
    "@capacitor/app": "6.0.3",
    "@capacitor/core": "6.2.2",
    "@capacitor/ios": "6.2.2"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.2",
    "sharp": "^0.34.3"
  }
}
